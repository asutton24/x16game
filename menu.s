title_level:
    lda #$D2
    sta $2
    lda #$D
    sta $3
    lda #$0
    sta $4
    lda #$A0
    sta $5
    lda #$0
    jsr set_level_base
    jsr mem_cpy
    lda #$0
    jsr full_level_load
    lda #$9
    sta $2
    lda #$28
    sta $3
    ldx #$1E
    ldy #$10
    jsr print_at
    lda $34
    cmp #$1
    beq no_continue
    lda #$C0
    sta $2
    lda #$0
    sta $3
    sta $4
    lda #$2
    sta $5
    jsr level_exit_init
    lda #$2E
    sta $2
    lda #$9
    sta $3
    ldy #$10
    ldx #$2
    jsr print_at
no_continue:
    lda #$99
    jsr set_clock
    lda #$FF
    sta $35
    jmp frame_loop_start
title_out:
    lda $8803
    sta $2
    lda $8804
    sta $3
    lda #$7C
    sta $4
    lda #$2
    sta $5
    jsr cmp_sixteen
    bcc continue_stage
    lda #$1
    sta $34
    jmp stage_starter
continue_stage:
     lda $34
     jmp stage_starter
